import type { EventInvitationRow, EventMemberRow, EventRole, EventRow } from "../domain";
import { eventTypeFieldLabel, eventTypeLabel, normalizeEventType } from "../event-types";
import { eventTemplateFor, eventTemplateText } from "../event-templates";
import type { Locale } from "../i18n";
import { esc, formatDateTime, formatEventDates } from "../utils";
import { bulkSelectionScript, cards, galleryFilterControls, galleryFilterScript, galleryProgressiveControls, galleryProgressiveScript, lightboxMarkup, mediaLikesScript, type MediaCardRow } from "./media";
import { mediaCommentsOverlay } from "./experience";
import { eventPinDialog, renderEventPinControl } from "./event-pin";
import { locationPickerMarkup, locationPickerScript } from "./location-picker";
import { shareIconButtons } from "./share";
import { eventHeader, logoutScript, page } from "./shared";

type RemovalRequest = {
  id: string;
  media_id: string;
  requester_email: string;
  reason: string;
  created_at: number;
};

type EventWorkspaceInput = {
  locale: Locale;
  event: EventRow;
  user: { name: string; email: string };
  membership: EventRole;
  items: MediaCardRow[];
  members: EventMemberRow[];
  invitations: EventInvitationRow[];
  removalRequests: RemovalRequest[];
  guestUrl: string;
  officialUrl: string;
  weddingUrl: string | null;
  guestQrSvg: string;
  officialQrSvg: string;
  weddingQrSvg: string | null;
  coverSourceMediaId: string | null;
  coverUpdatedAt: number | null;
};

const icon = (paths: string, className = "h-5 w-5") =>
  `<svg aria-hidden="true" viewBox="0 0 24 24" class="${className}" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">${paths}</svg>`;

export function renderEventWorkspace(input: EventWorkspaceInput) {
  const { locale, event, user, membership, items, members, invitations, removalRequests, guestUrl, officialUrl, weddingUrl, guestQrSvg, officialQrSvg, weddingQrSvg, coverSourceMediaId, coverUpdatedAt } = input;
  const el = locale === "el";
  const canManageMedia = membership === "owner" || membership === "editor";
  const canManageEvent = membership === "owner";
  const photoItems = items.filter((item) => item.media_type === "image");
  const imageCount = photoItems.length;
  const roleLabel = membership === "owner" ? (el ? "Ιδιοκτήτης" : "Owner") : membership === "editor" ? (el ? "Διαχειριστής" : "Manager") : (el ? "Θεατής" : "Viewer");
  const currentEventType = normalizeEventType(event.event_type);
  const previewLabel = currentEventType === "wedding" ? (el ? "Preview website" : "Preview website") : "Preview album";
  const previewUrl = weddingUrl ? `${weddingUrl}?preview=1&lang=${locale}&source=dashboard` : guestUrl;
  const templateNavLabel: Record<Locale, string> = {
    en: currentEventType === "wedding" ? "Wedding setup" : "Event setup",
    el: currentEventType === "wedding" ? "Οργάνωση γάμου" : "Οργάνωση event",
    fr: currentEventType === "wedding" ? "Configuration mariage" : "Configuration événement",
    de: currentEventType === "wedding" ? "Hochzeit einrichten" : "Event einrichten",
    es: currentEventType === "wedding" ? "Configurar boda" : "Configurar evento",
    it: currentEventType === "wedding" ? "Configura matrimonio" : "Configura evento",
  };
  const navItems = [
    ...(canManageEvent ? [["template", templateNavLabel[locale]]] : []),
    ...(canManageEvent ? [["engagement", el ? "Εμπειρία καλεσμένων" : "Guest experience"]] : []),
    ["share", el ? "Κοινοποίηση" : "Share"],
    ["gallery", el ? "Υλικό event" : "Event media"],
    ...(canManageEvent ? [["people", el ? "Πρόσβαση" : "Access"]] : []),
    ...(canManageEvent && removalRequests.length ? [["requests", el ? "Αιτήματα" : "Requests"]] : []),
    ...(canManageEvent ? [["danger", el ? "Διαγραφή" : "Delete"]] : []),
  ];
  const sectionNav = `<nav aria-label="${el ? "Ενότητες event" : "Event sections"}" class="sticky top-0 z-30 -mx-4 border-y border-[#e4ece8] bg-[#f3f7f5]/95 px-4 py-3 backdrop-blur-xl sm:mx-0 sm:rounded-2xl sm:border sm:px-3"><div class="flex gap-1 overflow-x-auto">${navItems.map(([id, label]) => `<a href="#${id}" class="whitespace-nowrap rounded-xl px-4 py-2 text-sm font-semibold text-[#586c65] transition hover:bg-white hover:text-[#183c33] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#75a895]">${label}</a>`).join("")}</div></nav>`;

  const ownerSelectionScript = bulkSelectionScript({
    selectButtonId: "owner-select-media",
    cardSelector: ".selectable-media",
    selectorSelector: ".media-selector",
    checkboxSelector: ".media-select",
    tickSelector: ".selection-tick",
    selectText: el ? "Επιλογή" : "Select",
    cancelText: el ? "Ακύρωση" : "Cancel",
    actions: [
      { buttonId: "owner-download-selected", label: el ? "Λήψη επιλεγμένων" : "Download selected", kind: "download" },
      ...(canManageMedia ? [{ buttonId: "owner-delete-selected", label: el ? "Διαγραφή επιλεγμένων" : "Delete selected", kind: "submit" as const, formId: "owner-bulk-media", inputId: "owner-media-ids", confirmMessage: el ? "Μεταφορά των επιλεγμένων στον κάδο;" : "Move selected media to trash?" }] : []),
    ],
  });

  const roleOptions = (current: "viewer" | "editor" | "professional") => [
    ["viewer", el ? "Θεατής" : "Viewer"],
    ["editor", el ? "Διαχειριστής" : "Manager"],
    ["professional", el ? "Επαγγελματίας / επίσημος φωτογράφος" : "Professional / official photographer"],
  ].map(([value, label]) => `<option value="${value}"${current === value ? " selected" : ""}>${label}</option>`).join("");
  const memberRows = members.map((member) => {
    if (member.role === "owner") return `<div class="flex items-center justify-between gap-3 rounded-2xl border border-[#e3ebe7] bg-white p-4"><div class="flex min-w-0 items-center gap-3"><span class="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-[#e4f0eb] text-sm font-bold text-[#214c40]">${esc((member.name || member.email).slice(0, 1).toUpperCase())}</span><div class="min-w-0"><p class="truncate font-semibold text-[#183c33]">${esc(member.name)}</p><p class="truncate text-sm text-[#6c7b76]">${esc(member.email)}</p></div></div><span class="rounded-full bg-[#e8f3ee] px-3 py-1 text-xs font-semibold text-[#255848]">Owner</span></div>`;
    const status = member.role === "professional" && member.access_status === "invited" ? `<span class="text-xs font-semibold text-amber-700">${el ? "Αναμονή αποδοχής" : "Awaiting acceptance"}</span>` : "";
    return `<div class="rounded-2xl border border-[#e3ebe7] bg-white p-4"><div class="flex min-w-0 items-center gap-3"><span class="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-[#e4f0eb] text-sm font-bold text-[#214c40]">${esc((member.name || member.email).slice(0, 1).toUpperCase())}</span><div class="min-w-0"><p class="truncate font-semibold text-[#183c33]">${esc(member.name)}</p><p class="truncate text-sm text-[#6c7b76]">${esc(member.email)}</p></div></div><div class="mt-4 flex flex-col gap-2 sm:flex-row sm:items-center"><form action="/api/account/events/${event.code}/members/role" method="post" class="min-w-0 flex-1"><input type="hidden" name="userId" value="${esc(member.user_id)}"><input type="hidden" name="locale" value="${locale}"><label class="sr-only" for="role-${esc(member.user_id)}">${el ? "Ρόλος" : "Role"}</label><select id="role-${esc(member.user_id)}" name="role" onchange="this.form.requestSubmit()" class="w-full rounded-xl border border-[#d6e0dc] bg-white px-3 py-2.5 text-sm font-semibold text-[#344941]">${roleOptions(member.role)}</select><button class="sr-only">${el ? "Αποθήκευση ρόλου" : "Save role"}</button></form>${status}<form action="/api/account/events/${event.code}/members/remove" method="post" onsubmit="return confirm('${el ? "Να αφαιρεθεί εντελώς η πρόσβαση αυτού του ατόμου;" : "Remove this person and all of their event access?"}')"><input type="hidden" name="userId" value="${esc(member.user_id)}"><input type="hidden" name="locale" value="${locale}"><button class="rounded-xl px-3 py-2.5 text-sm font-semibold text-red-700 hover:bg-red-50">${el ? "Αφαίρεση" : "Remove"}</button></form></div></div>`;
  }).join("");
  const inviteRows = invitations.map((invite) => {
    const current = invite.invitation_kind === "professional" ? "professional" : invite.role;
    return `<div class="rounded-2xl border border-dashed border-[#ccdcd5] bg-[#fafcfb] p-4"><div class="min-w-0"><p class="truncate font-semibold text-[#2b443c]">${esc(invite.email)}</p><p class="mt-0.5 text-xs font-semibold text-amber-700">${el ? "Πρόσκληση σε αναμονή" : "Pending invitation"}</p></div><div class="mt-4 flex flex-col gap-2 sm:flex-row sm:items-center"><form action="/api/account/events/${event.code}/members/role" method="post" class="min-w-0 flex-1"><input type="hidden" name="invitationId" value="${esc(invite.id)}"><input type="hidden" name="locale" value="${locale}"><label class="sr-only" for="invite-role-${esc(invite.id)}">${el ? "Ρόλος πρόσκλησης" : "Invitation role"}</label><select id="invite-role-${esc(invite.id)}" name="role" onchange="this.form.requestSubmit()" class="w-full rounded-xl border border-[#d6e0dc] bg-white px-3 py-2.5 text-sm font-semibold text-[#344941]">${roleOptions(current)}</select><button class="sr-only">${el ? "Αποθήκευση ρόλου" : "Save role"}</button></form><form action="/api/account/events/${event.code}/members/remove" method="post"><input type="hidden" name="invitationId" value="${esc(invite.id)}"><input type="hidden" name="locale" value="${locale}"><button class="rounded-xl px-3 py-2.5 text-sm font-semibold text-red-700 hover:bg-red-50">${el ? "Ακύρωση" : "Cancel"}</button></form></div></div>`;
  }).join("");

  const shareCard = (title: string, description: string, url: string, qr: string, id: string, kind: "website" | "guest" | "official") => {
    const kindLabel = kind === "website" ? (el ? "Wedding website" : "Wedding website") : kind === "official" ? (el ? "Επίσημο album" : "Official album") : (el ? "Guest gallery" : "Guest gallery");
    return `<article class="flex h-full min-w-0 flex-col rounded-[1.75rem] border border-[#dce7e2] bg-white p-5 text-center shadow-sm transition hover:-translate-y-0.5 hover:shadow-md sm:p-6"><div class="mx-auto w-full max-w-[142px] rounded-[1.35rem] border border-[#d8e4df] bg-white p-3 shadow-[0_12px_28px_rgba(24,60,51,.09)]">${qr}</div><div class="mt-5 flex flex-1 flex-col items-center"><span class="inline-flex rounded-full bg-[#e8f3ee] px-3 py-1 text-[11px] font-bold uppercase tracking-[.12em] text-[#255848]">${kindLabel}</span><h3 class="mt-3 text-2xl leading-tight text-[#183c33]">${title}</h3><p class="mt-2 max-w-sm text-sm leading-6 text-[#687a74]">${description}</p><a href="${esc(url)}${url.includes("?") ? "&" : "?"}lang=${locale}" target="_blank" rel="noopener" class="mt-4 block w-full truncate rounded-xl bg-[#f3f7f5] px-3 py-2.5 text-left text-xs font-semibold text-[#2f6b5b] hover:bg-[#eaf2ee]" title="${esc(url)}">${esc(url)}</a><div class="mt-auto flex flex-wrap justify-center gap-2 pt-4"><button type="button" data-copy-link="${id}" class="inline-flex min-h-11 items-center rounded-xl bg-[#183c33] px-4 py-2.5 text-sm font-semibold text-white">${icon('<rect x="9" y="9" width="11" height="11" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>')}<span class="ml-2">${el ? "Αντιγραφή" : "Copy link"}</span></button><a href="${esc(url)}${url.includes("?") ? "&" : "?"}lang=${locale}" target="_blank" rel="noopener" class="inline-flex min-h-11 items-center rounded-xl border border-[#d9e5e0] px-4 py-2.5 text-sm font-semibold text-[#2b443c] hover:bg-[#f6f9f7]">Preview</a></div>${shareIconButtons(url, title, locale, false)}<input id="${id}" type="hidden" value="${esc(url)}"></div></article>`;
  };


  const pencil = icon('<path d="M12 20h9"/><path d="M16.5 3.5a2.12 2.12 0 0 1 3 3L7 19l-4 1 1-4Z"/>', "h-4 w-4");
  const inlineEditScript = canManageEvent ? `<script>(()=>{document.querySelectorAll('[data-inline-editor]').forEach(editor=>{const display=editor.querySelector('[data-inline-display]'),form=editor.querySelector('form'),open=editor.querySelector('[data-inline-open]'),cancel=editor.querySelector('[data-inline-cancel]'),value=editor.querySelector('[data-inline-value]'),error=editor.querySelector('[data-inline-error]');const close=()=>{form.classList.add('hidden');display.classList.remove('hidden');error?.classList.add('hidden')};open?.addEventListener('click',()=>{display.classList.add('hidden');form.classList.remove('hidden');form.querySelector('input:not([type=hidden])')?.focus()});cancel?.addEventListener('click',close);form?.addEventListener('submit',async event=>{event.preventDefault();const submit=form.querySelector('button[type=submit]');submit.disabled=true;error?.classList.add('hidden');try{const response=await fetch(form.action,{method:'POST',credentials:'include',headers:{Accept:'application/json'},body:new FormData(form)}),data=await response.json().catch(()=>null);if(!response.ok||!data)throw new Error(data?.message||'save_failed');if(value)value.textContent=form.dataset.field==='name'?data.eventName:form.dataset.field==='dates'?data.eventDates:(data.eventLocation||${JSON.stringify(el ? "Δεν ορίστηκε τοποθεσία" : "Location not set")});close()}catch{if(error){error.textContent=${JSON.stringify(el ? "Δεν αποθηκεύτηκε. Έλεγξε τα στοιχεία." : "Could not save. Check the details.")};error.classList.remove('hidden')}}finally{submit.disabled=false}})})})()<\/script>` : "";

  const settings = canManageEvent ? `<section id="settings" class="scroll-mt-24 rounded-[2rem] border border-[#dee7e3] bg-white p-5 shadow-sm sm:p-7"><div class="max-w-2xl"><p class="text-xs font-bold uppercase tracking-[.18em] text-[#2f6b5b]">${el ? "Βασικά στοιχεία" : "Event details"}</p><h2 class="mt-2 text-3xl text-[#183c33]">${el ? "Όνομα και ημερομηνίες" : "Name and dates"}</h2><p class="mt-2 text-sm leading-6 text-[#687a74]">${el ? "Αυτές είναι οι ημερομηνίες του ίδιου του event, όχι η ημερομηνία δημιουργίας του album." : "These are the dates of the event itself, not the album creation date."}</p></div><form action="/api/account/events/${event.code}/details" method="post" class="mt-6 grid gap-4 md:grid-cols-2"><input type="hidden" name="locale" value="${locale}"><label class="text-sm font-semibold text-[#344941] md:col-span-2">${el ? "Όνομα event" : "Event name"}<input name="eventName" required maxlength="100" value="${esc(event.eventName)}" class="mt-2 w-full rounded-xl border border-[#d6e0dc] bg-white px-4 py-3 font-normal text-[#183c33] outline-none focus:border-[#3f7d6c] focus:ring-2 focus:ring-[#c8ddd5]"></label><label class="text-sm font-semibold text-[#344941]">${el ? "Έναρξη" : "Start date"}<input name="eventStartDate" type="date" required value="${esc(event.event_start_date || "")}" class="mt-2 w-full rounded-xl border border-[#d6e0dc] px-4 py-3 font-normal outline-none focus:border-[#3f7d6c] focus:ring-2 focus:ring-[#c8ddd5]"></label><label class="text-sm font-semibold text-[#344941]">${el ? "Λήξη" : "End date"}<input name="eventEndDate" type="date" value="${esc(event.event_end_date || "")}" class="mt-2 w-full rounded-xl border border-[#d6e0dc] px-4 py-3 font-normal outline-none focus:border-[#3f7d6c] focus:ring-2 focus:ring-[#c8ddd5]"></label><div class="md:col-span-2"><button class="rounded-xl bg-[#2f6b5b] px-6 py-3 font-semibold text-white shadow-sm transition hover:bg-[#255848]">${el ? "Αποθήκευση αλλαγών" : "Save changes"}</button></div></form><div class="my-7 border-t border-[#e6ede9]"></div><div class="grid gap-6 lg:grid-cols-[1fr_auto]"><div><h3 class="text-xl font-semibold text-[#183c33]">${el ? "Ιδιωτικότητα και PIN" : "Privacy and PIN"}</h3><p class="mt-2 text-sm text-[#687a74]">${event.gallery_pin_hash ? (el ? "Το guest gallery προστατεύεται με PIN." : "The guest gallery is protected by a PIN.") : (el ? "Το guest gallery δεν έχει ενεργό PIN." : "The guest gallery has no active PIN.")}</p></div><span class="inline-flex h-fit items-center rounded-full ${event.gallery_pin_hash ? "bg-emerald-50 text-emerald-800" : "bg-[#f1f5f3] text-[#586c65]"} px-3 py-1.5 text-xs font-bold">${event.gallery_pin_hash ? (el ? "PIN ενεργό" : "PIN enabled") : (el ? "Χωρίς PIN" : "No PIN")}</span></div><form action="/api/account/events/${event.code}/privacy" method="post" class="mt-4 flex flex-col gap-3 sm:flex-row"><input type="hidden" name="locale" value="${locale}"><input name="pin" inputmode="numeric" pattern="[0-9]{4,8}" minlength="4" maxlength="8" autocomplete="off" placeholder="${el ? "Νέο PIN 4–8 ψηφίων" : "New 4–8 digit PIN"}" class="min-w-0 flex-1 rounded-xl border border-[#d6e0dc] px-4 py-3 outline-none focus:border-[#3f7d6c] focus:ring-2 focus:ring-[#c8ddd5]"><button name="action" value="set" class="rounded-xl bg-[#183c33] px-5 py-3 font-semibold text-white">${event.gallery_pin_hash ? (el ? "Αλλαγή PIN" : "Change PIN") : (el ? "Ορισμός PIN" : "Set PIN")}</button>${event.gallery_pin_hash ? `<button name="action" value="remove" class="rounded-xl border border-red-200 px-5 py-3 font-semibold text-red-700 hover:bg-red-50">${el ? "Αφαίρεση" : "Remove"}</button>` : ""}</form></section>` : "";

  const heroPinControl = canManageEvent ? `<div class="pointer-events-none relative z-20 h-0"><div class="pointer-events-auto absolute right-5 top-5 sm:right-8 sm:top-8">${renderEventPinControl(event, locale)}</div></div>` : "";
  const locationEditor = `<div data-inline-editor class="rounded-2xl border border-[#dfe8e4] bg-white px-4 py-3 shadow-sm"><div data-inline-display class="flex items-center justify-between gap-3"><div class="flex min-w-0 items-center gap-2 text-sm text-[#586c65]">${icon('<path d="M20 10c0 5-8 12-8 12S4 15 4 10a8 8 0 1 1 16 0Z"/><circle cx="12" cy="10" r="2.5"/>', "h-4 w-4 shrink-0")}<span data-inline-value class="truncate">${esc(event.location || (el ? "Δεν ορίστηκε τοποθεσία" : "Location not set"))}</span></div>${canManageEvent ? `<button type="button" data-inline-open aria-label="${el ? "Επεξεργασία τοποθεσίας" : "Edit location"}" class="flex h-8 w-8 shrink-0 items-center justify-center rounded-full border border-[#d6e0dc] text-[#2f6b5b] transition hover:bg-[#e8f3ee]">${pencil}</button>` : ""}</div>${canManageEvent ? `<form data-field="location" action="/api/account/events/${event.code}/details" method="post" class="hidden"><input type="hidden" name="locale" value="${locale}"><input type="hidden" name="eventName" value="${esc(event.eventName)}"><input type="hidden" name="eventStartDate" value="${esc(event.event_start_date || "")}"><input type="hidden" name="eventEndDate" value="${esc(event.event_end_date || event.event_start_date || "")}"><div class="mt-2 flex flex-col gap-2 sm:flex-row"><input name="location" maxlength="160" value="${esc(event.location || "")}" placeholder="${el ? "π.χ. Σαντορίνη, Ελλάδα" : "e.g. Santorini, Greece"}" class="min-w-0 flex-1 rounded-xl border border-[#d6e0dc] px-4 py-2.5"><button type="submit" class="rounded-xl bg-[#183c33] px-4 py-2.5 text-sm font-semibold text-white">${el ? "Αποθήκευση" : "Save"}</button><button type="button" data-inline-cancel class="rounded-xl border border-[#d6e0dc] px-4 py-2.5 text-sm font-semibold">${el ? "Ακύρωση" : "Cancel"}</button></div><p data-inline-error class="mt-2 hidden text-sm text-red-700"></p></form>` : ""}</div>`;
  const lockedTypeText: Record<Locale, string> = {
    en: "Chosen when the event was created · cannot be changed",
    el: "Επιλέχθηκε κατά τη δημιουργία · δεν μπορεί να αλλάξει",
    fr: "Choisi à la création · non modifiable",
    de: "Bei der Erstellung gewählt · nicht änderbar",
    es: "Elegido al crear el evento · no se puede cambiar",
    it: "Scelto alla creazione · non modificabile",
  };
  const eventTypeEditor = `<div data-event-type-locked class="rounded-2xl border border-[#dfe8e4] bg-white px-4 py-3 shadow-sm"><div class="flex items-center gap-3"><span class="text-[#2f6b5b]">${icon('<rect x="5" y="10" width="14" height="10" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3"/>', "h-4 w-4 shrink-0")}</span><div class="min-w-0"><p class="text-xs font-semibold text-[#72827c]">${eventTypeFieldLabel(locale)}</p><p class="truncate text-sm font-bold text-[#2b443c]">${esc(eventTypeLabel(currentEventType, locale))}</p></div></div><p class="mt-2 text-xs leading-5 text-[#72827c]">${lockedTypeText[locale]}</p></div>`;
  const template = eventTemplateFor(currentEventType);
  const templateComplete = new Set<string>([
    ...(event.eventName && event.event_start_date && event.location ? ["basics"] : []),
    ...(imageCount > 0 ? ["media"] : []),
    ...(members.length > 1 || invitations.length > 0 ? ["people"] : []),
  ]);
  const templateStatus: Record<Locale, { complete: string; ready: string; next: string }> = {
    en: { complete: "Complete", ready: "Ready", next: "Coming next" },
    el: { complete: "Ολοκληρώθηκε", ready: "Έτοιμο", next: "Επόμενη προσθήκη" },
    fr: { complete: "Terminé", ready: "Prêt", next: "Prochainement" },
    de: { complete: "Erledigt", ready: "Bereit", next: "Demnächst" },
    es: { complete: "Completado", ready: "Listo", next: "Próximamente" },
    it: { complete: "Completato", ready: "Pronto", next: "Prossimamente" },
  };
  const templateChecklist = canManageEvent ? `<section id="template" data-event-template="${template.key}" class="mt-6 scroll-mt-24 overflow-hidden rounded-[2rem] border border-[#ceddd7] bg-white shadow-sm"><div class="bg-[#e9f1ed] p-5 sm:p-7"><div class="flex flex-wrap items-start justify-between gap-4"><div class="max-w-3xl"><p class="text-xs font-bold uppercase tracking-[.18em] text-[#2f6b5b]">${esc(eventTemplateText(template.eyebrow, locale))}</p><h2 class="mt-2 text-3xl text-[#183c33]">${esc(eventTemplateText(template.title, locale))}</h2><p class="mt-2 text-sm leading-6 text-[#5f716a]">${esc(eventTemplateText(template.description, locale))}</p></div><div class="flex flex-wrap gap-2"><span class="rounded-full border border-[#bfd2ca] bg-white/80 px-3 py-1.5 text-xs font-bold text-[#255848]">${esc(eventTypeLabel(currentEventType, locale))}</span>${currentEventType === "wedding" && weddingUrl ? `<a href="/dashboard/${event.code}/wedding/setup?lang=${locale}&step=1" class="rounded-xl bg-[#183c33] px-4 py-2 text-sm font-semibold text-white">${el ? "Edit website" : "Edit website"}</a><a href="${esc(weddingUrl)}?lang=${locale}" target="_blank" rel="noopener" class="rounded-xl border border-[#afc8be] bg-white px-4 py-2 text-sm font-semibold text-[#255848]">${el ? "Preview" : "Preview"}</a>` : ""}</div></div></div><ol class="grid gap-3 p-5 sm:grid-cols-2 sm:p-7 xl:grid-cols-3">${template.items.map((item, index) => {
    const complete = templateComplete.has(item.id);
    const status = complete ? templateStatus[locale].complete : item.href ? templateStatus[locale].ready : templateStatus[locale].next;
    const target = item.href === "wedding-profile"
      ? `/dashboard/${event.code}/wedding/setup?lang=${locale}&step=1`
      : item.href === "wedding-schedule"
        ? `/dashboard/${event.code}/wedding/setup?lang=${locale}&step=2`
        : item.href === "wedding-features"
          ? `/dashboard/${event.code}/wedding/setup?lang=${locale}&step=4`
          : item.href ? `#${item.href}` : null;
    const inner = `<span class="flex h-9 w-9 shrink-0 items-center justify-center rounded-full ${complete ? "bg-[#2f6b5b] text-white" : item.href ? "bg-[#e5efeb] text-[#255848]" : "bg-[#f1f3f2] text-[#87958f]"} text-sm font-bold">${complete ? "✓" : index + 1}</span><span class="min-w-0"><span class="block font-semibold text-[#183c33]">${esc(eventTemplateText(item.title, locale))}</span><span class="mt-1 block text-sm leading-5 text-[#687a74]">${esc(eventTemplateText(item.description, locale))}</span><span class="mt-3 inline-flex rounded-full ${complete ? "bg-emerald-50 text-emerald-800" : item.href ? "bg-[#edf4f1] text-[#2f6b5b]" : "bg-[#f3f4f3] text-[#77857f]"} px-2.5 py-1 text-[11px] font-bold uppercase tracking-[.08em]">${status}</span></span>`;
    return target ? `<li><a href="${esc(target)}" class="flex h-full gap-3 rounded-2xl border border-[#e0e9e5] p-4 transition hover:-translate-y-0.5 hover:border-[#adc9be] hover:shadow-sm">${inner}</a></li>` : `<li class="flex gap-3 rounded-2xl border border-dashed border-[#d9e1dd] bg-[#fafbfa] p-4">${inner}</li>`;
  }).join("")}</ol></section>` : "";
  const detailsSettings = canManageEvent ? `<section id="settings" class="scroll-mt-24 rounded-[2rem] border border-[#dee7e3] bg-white p-5 shadow-sm sm:p-7"><div class="max-w-2xl"><p class="text-xs font-bold uppercase tracking-[.18em] text-[#2f6b5b]">${el ? "Βασικά στοιχεία" : "Event details"}</p><h2 class="mt-2 text-3xl text-[#183c33]">${el ? "Όνομα, ημερομηνίες και τοποθεσία" : "Name, dates and location"}</h2><p class="mt-2 text-sm leading-6 text-[#687a74]">${el ? "Ενημέρωσε τα στοιχεία του ίδιου του event, όχι τα στοιχεία δημιουργίας του album." : "Update the event itself, not the album creation details."}</p></div><form action="/api/account/events/${event.code}/details" method="post" class="mt-6 grid gap-4 md:grid-cols-2"><input type="hidden" name="locale" value="${locale}"><label class="text-sm font-semibold text-[#344941] md:col-span-2">${el ? "Όνομα event" : "Event name"}<input name="eventName" required maxlength="100" value="${esc(event.eventName)}" class="mt-2 w-full rounded-xl border border-[#d6e0dc] px-4 py-3 font-normal"></label><label class="text-sm font-semibold text-[#344941] md:col-span-2">${el ? "Τοποθεσία" : "Location"}<input name="location" maxlength="160" value="${esc(event.location || "")}" placeholder="${el ? "π.χ. Σαντορίνη, Ελλάδα" : "e.g. Santorini, Greece"}" class="mt-2 w-full rounded-xl border border-[#d6e0dc] px-4 py-3 font-normal"></label><label class="text-sm font-semibold text-[#344941]">${el ? "Έναρξη" : "Start date"}<input name="eventStartDate" type="date" required value="${esc(event.event_start_date || "")}" class="mt-2 w-full rounded-xl border border-[#d6e0dc] px-4 py-3 font-normal"></label><label class="text-sm font-semibold text-[#344941]">${el ? "Λήξη" : "End date"}<input name="eventEndDate" type="date" value="${esc(event.event_end_date || "")}" class="mt-2 w-full rounded-xl border border-[#d6e0dc] px-4 py-3 font-normal"></label><div class="md:col-span-2"><button class="rounded-xl bg-[#2f6b5b] px-6 py-3 font-semibold text-white shadow-sm transition hover:bg-[#255848]">${el ? "Αποθήκευση αλλαγών" : "Save changes"}</button></div></form></section>` : "";

  const people = canManageEvent ? `<section id="people" class="scroll-mt-24 rounded-[2rem] border border-[#dee7e3] bg-white p-5 shadow-sm sm:p-7"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-[#2f6b5b]">${el ? "Πρόσβαση" : "Access"}</p><h2 class="mt-2 text-3xl text-[#183c33]">${el ? "Άτομα και ρόλοι" : "People and roles"}</h2><p class="mt-2 max-w-3xl text-sm leading-6 text-[#687a74]">${el ? "Διαχειρίσου όλους από ένα σημείο. Η αλλαγή ρόλου εφαρμόζεται αμέσως μόνο σε αυτό το event." : "Manage everyone in one place. Role changes apply immediately to this event only."}</p></div><div class="mt-5 grid gap-2 text-xs sm:grid-cols-3"><p class="rounded-xl bg-[#f7faf8] p-3 text-[#586c65]"><strong class="block text-[#2b443c]">${el ? "Θεατής" : "Viewer"}</strong>${el ? "Προβολή και λήψη." : "View and download."}</p><p class="rounded-xl bg-[#f7faf8] p-3 text-[#586c65]"><strong class="block text-[#2b443c]">${el ? "Διαχειριστής" : "Manager"}</strong>${el ? "Διαχείριση του guest gallery." : "Manage the guest gallery."}</p><p class="rounded-xl bg-[#f7faf8] p-3 text-[#586c65]"><strong class="block text-[#2b443c]">Professional</strong>${el ? "Studio και official album." : "Studio and official album."}</p></div><div class="mt-6 grid gap-7 lg:grid-cols-[minmax(0,1.25fr)_minmax(300px,.75fr)]"><div class="space-y-3">${memberRows}${inviteRows || ""}</div><div class="h-fit rounded-2xl bg-[#f4f8f6] p-5"><h3 class="text-xl font-semibold text-[#183c33]">${el ? "Πρόσκληση ατόμου" : "Invite someone"}</h3><form action="/api/account/events/${event.code}/invite" method="post" class="mt-4 space-y-3"><input type="hidden" name="locale" value="${locale}"><input name="email" type="email" required autocomplete="email" placeholder="name@example.com" class="w-full rounded-xl border border-[#d6e0dc] bg-white px-4 py-3 outline-none focus:border-[#3f7d6c] focus:ring-2 focus:ring-[#c8ddd5]"><label class="block text-sm font-semibold text-[#344941]">${el ? "Ρόλος" : "Role"}<select name="role" class="mt-2 w-full rounded-xl border border-[#d6e0dc] bg-white px-4 py-3 font-normal"><option value="viewer">${el ? "Θεατής — προβολή και λήψη" : "Viewer — view and download"}</option><option value="editor">${el ? "Διαχειριστής — διαχείριση gallery" : "Manager — manage gallery"}</option><option value="professional">${el ? "Professional — επίσημος φωτογράφος" : "Professional — official photographer"}</option></select></label><p class="text-xs leading-5 text-[#6c7b76]">${el ? "Ο Professional αποκτά πρόσβαση στο Studio και στο official album. Μπορείς να αλλάξεις τον ρόλο αργότερα από τη λίστα." : "A Professional receives Studio and official-album access. You can change the role later from the list."}</p><button class="w-full rounded-xl bg-[#2f6b5b] px-5 py-3 font-semibold text-white hover:bg-[#255848]">${el ? "Αποστολή πρόσκλησης" : "Send invitation"}</button></form></div></div></section>` : "";

  const requests = canManageEvent && removalRequests.length ? `<section id="requests" class="scroll-mt-24 rounded-[2rem] border border-[#dee7e3] bg-white p-5 shadow-sm sm:p-7"><h2 class="text-3xl text-[#183c33]">${el ? "Αιτήματα αφαίρεσης" : "Removal requests"}</h2><div class="mt-5 space-y-3">${removalRequests.map((request) => `<article class="rounded-2xl border border-[#e3ebe7] p-4"><p class="text-sm text-[#6c7b76]">${esc(request.requester_email)} · ${formatDateTime(request.created_at, locale)}</p><p class="mt-2 text-[#2b443c]">${esc(request.reason)}</p><div class="mt-4 flex flex-wrap gap-2"><form action="/api/account/events/${event.code}/removal/${request.id}/approve" method="post"><input type="hidden" name="locale" value="${locale}"><button class="rounded-xl bg-red-700 px-4 py-2 text-sm font-semibold text-white">${el ? "Αφαίρεση φωτογραφίας" : "Remove media"}</button></form><form action="/api/account/events/${event.code}/removal/${request.id}/dismiss" method="post"><input type="hidden" name="locale" value="${locale}"><button class="rounded-xl border border-[#d6e0dc] px-4 py-2 text-sm font-semibold">${el ? "Απόρριψη" : "Dismiss"}</button></form></div></article>`).join("")}</div></section>` : "";

  const danger = canManageEvent ? `<section id="danger" class="rounded-[2rem] border border-red-200 bg-red-50/70 p-5 sm:p-7"><div class="flex flex-col justify-between gap-5 sm:flex-row sm:items-center"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-red-700">${el ? "Επικίνδυνη ζώνη" : "Danger zone"}</p><h2 class="mt-2 text-2xl text-[#7f1d1d]">${el ? "Διαγραφή event" : "Delete event"}</h2><p class="mt-2 max-w-2xl text-sm leading-6 text-red-900/75">${el ? "Το event και τα αρχεία του θα μεταφερθούν στον κάδο για 30 ημέρες. Μπορείς να τα επαναφέρεις πριν διαγραφούν οριστικά." : "The event and its files will move to Trash for 30 days. You can restore them before permanent deletion."}</p></div><form action="/api/account/events/${event.code}/trash" method="post" onsubmit="return confirm('${el ? "Μεταφορά ολόκληρου του event στον κάδο;" : "Move the entire event to Trash?"}')"><input type="hidden" name="locale" value="${locale}"><button class="whitespace-nowrap rounded-xl bg-red-700 px-5 py-3 font-semibold text-white hover:bg-red-800">${el ? "Μεταφορά στον κάδο" : "Move to Trash"}</button></form></div></section>` : "";

  const locationEnhancement = `<script>(()=>{const mount=()=>{const editor=document.querySelector('[data-event-metadata]'),actions=document.querySelector('#overview a[href="#share"]')?.parentElement;if(editor&&actions&&!actions.parentElement?.querySelector(':scope > [data-event-metadata]'))actions.before(editor)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount()})()<\/script>${canManageEvent ? `<template id="inline-location-template">${locationPickerMarkup({ id: "event-inline-location", locale, location: event, compact: true })}</template><script>(()=>{const enhance=()=>{const inline=document.querySelector('form[data-field="location"] input[name="location"]'),template=document.getElementById('inline-location-template');if(inline&&template)inline.replaceWith(template.content.cloneNode(true))};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',enhance,{once:true});else enhance()})()<\/script>${locationPickerScript(locale)}` : ""}`;
  const ownerGalleryCards = cards(photoItems, {
    lightbox: true,
    selectable: true,
    deferredSelection: true,
    coverControl: canManageEvent ? { eventCode: event.code, locale, activeMediaId: coverSourceMediaId } : undefined,
    deferAfter: 12,
  });
  const body = `${eventHeader(locale, user, "")}
  <main class="mx-auto max-w-7xl px-4 py-6 sm:px-6 sm:py-9">
    ${heroPinControl}
    <section id="overview" class="scroll-mt-24 overflow-hidden rounded-[2rem] bg-[#183c33] text-white shadow-xl"><div class="relative p-6 sm:p-8 lg:p-10">${coverUpdatedAt ? `<img src="/event-cover/${encodeURIComponent(event.code)}?v=${coverUpdatedAt}" alt="" class="absolute inset-0 h-full w-full object-cover opacity-25"><div class="absolute inset-0 bg-gradient-to-r from-[#183c33] via-[#183c33]/90 to-[#183c33]/45"></div>` : ""}<div class="absolute -right-20 -top-24 h-64 w-64 rounded-full bg-[#3f7d6c]/25 blur-3xl"></div><div class="relative"><div class="flex flex-wrap items-center gap-2"><span class="rounded-full border border-white/15 bg-white/10 px-3 py-1 text-xs font-bold uppercase tracking-[.12em]">${roleLabel}</span><span class="rounded-full border border-white/15 bg-white/10 px-3 py-1 text-xs font-semibold">${event.gallery_pin_hash ? (el ? "PIN ενεργό" : "PIN enabled") : (el ? "Public link" : "Public link")}</span>${coverSourceMediaId ? `<span class="rounded-full border border-white/15 bg-white/10 px-3 py-1 text-xs font-semibold">${el ? "Cover αποθηκευμένο" : "Cover saved"}</span>` : ""}</div><div data-inline-editor class="mt-5"><div data-inline-display class="flex max-w-5xl items-start gap-2"><h1 data-inline-value class="max-w-4xl text-4xl leading-tight sm:text-5xl lg:text-6xl">${esc(event.eventName)}</h1>${canManageEvent ? `<button type="button" data-inline-open aria-label="${el ? "Επεξεργασία ονόματος" : "Edit event name"}" class="mt-1 flex h-9 w-9 shrink-0 items-center justify-center rounded-full border border-white/20 bg-white/10 text-white transition hover:bg-white/20">${pencil}</button>` : ""}</div>${canManageEvent ? `<form data-field="name" action="/api/account/events/${event.code}/details" method="post" class="hidden max-w-3xl rounded-2xl border border-white/15 bg-black/20 p-3 backdrop-blur"><input type="hidden" name="locale" value="${locale}"><input type="hidden" name="eventStartDate" value="${esc(event.event_start_date || "")}"><input type="hidden" name="eventEndDate" value="${esc(event.event_end_date || event.event_start_date || "")}"><input name="eventName" required maxlength="100" value="${esc(event.eventName)}" class="w-full rounded-xl border border-white/20 bg-white px-4 py-3 text-lg font-semibold text-[#183c33]"><p data-inline-error class="mt-2 hidden text-sm text-red-200"></p><div class="mt-3 flex gap-2"><button type="submit" class="rounded-xl bg-white px-4 py-2 text-sm font-semibold text-[#183c33]">${el ? "Αποθήκευση" : "Save"}</button><button type="button" data-inline-cancel class="rounded-xl border border-white/20 px-4 py-2 text-sm font-semibold text-white">${el ? "Ακύρωση" : "Cancel"}</button></div></form>` : ""}</div><div data-inline-editor class="mt-3"><div data-inline-display class="flex items-center gap-2"><p data-inline-value class="text-base text-[#cadbd3] sm:text-lg">${esc(formatEventDates(event, locale))}</p>${canManageEvent ? `<button type="button" data-inline-open aria-label="${el ? "Επεξεργασία ημερομηνιών" : "Edit event dates"}" class="flex h-8 w-8 shrink-0 items-center justify-center rounded-full border border-white/20 bg-white/10 text-white transition hover:bg-white/20">${pencil}</button>` : ""}</div>${canManageEvent ? `<form data-field="dates" action="/api/account/events/${event.code}/details" method="post" class="hidden max-w-xl rounded-2xl border border-white/15 bg-black/20 p-3 backdrop-blur"><input type="hidden" name="locale" value="${locale}"><input type="hidden" name="eventName" value="${esc(event.eventName)}"><div class="grid gap-2 sm:grid-cols-2"><label class="text-xs font-semibold text-white/75">${el ? "Έναρξη" : "Start date"}<input name="eventStartDate" type="date" required value="${esc(event.event_start_date || "")}" class="mt-1 w-full rounded-xl border-0 bg-white px-3 py-2.5 text-[#183c33]"></label><label class="text-xs font-semibold text-white/75">${el ? "Λήξη" : "End date"}<input name="eventEndDate" type="date" value="${esc(event.event_end_date || event.event_start_date || "")}" class="mt-1 w-full rounded-xl border-0 bg-white px-3 py-2.5 text-[#183c33]"></label></div><p data-inline-error class="mt-2 hidden text-sm text-red-200"></p><div class="mt-3 flex gap-2"><button type="submit" class="rounded-xl bg-white px-4 py-2 text-sm font-semibold text-[#183c33]">${el ? "Αποθήκευση" : "Save"}</button><button type="button" data-inline-cancel class="rounded-xl border border-white/20 px-4 py-2 text-sm font-semibold text-white">${el ? "Ακύρωση" : "Cancel"}</button></div></form>` : ""}</div><div class="mt-7 flex flex-wrap gap-3"><a href="${esc(previewUrl)}?lang=${locale}" target="_blank" rel="noopener" class="inline-flex items-center rounded-xl bg-white px-5 py-3 font-semibold text-[#183c33] shadow-sm">${icon('<path d="M2 12s3.5-6 10-6 10 6 10 6-3.5 6-10 6S2 12 2 12Z"/><circle cx="12" cy="12" r="2.5"/>')}<span class="ml-2">${previewLabel}</span></a><a href="#share" class="inline-flex items-center rounded-xl border border-white/20 bg-white/10 px-5 py-3 font-semibold text-white hover:bg-white/15">${icon('<circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><path d="m8.6 10.5 6.8-4M8.6 13.5l6.8 4"/>')}<span class="ml-2">${el ? "Κοινοποίηση" : "Share"}</span></a></div></div></div></section>
    <div data-event-metadata class="mt-4 grid max-w-3xl gap-3 sm:grid-cols-2"><div data-event-type-editor>${eventTypeEditor}</div><div data-event-location-editor>${locationEditor}</div></div>
    <div class="mt-4">${sectionNav}</div>
    ${templateChecklist}
    <section class="mt-6 grid gap-4 sm:grid-cols-2"><article class="rounded-2xl border border-[#dee7e3] bg-white p-5"><p class="text-sm font-semibold text-[#6c7b76]">${el ? "Φωτογραφίες" : "Photos"}</p><p class="mt-2 text-3xl text-[#183c33]">${imageCount}</p></article><article class="rounded-2xl border border-[#dee7e3] bg-white p-5"><p class="text-sm font-semibold text-[#6c7b76]">${el ? "Πρόσβαση" : "Access"}</p><p class="mt-2 text-xl font-semibold text-[#183c33]">${roleLabel}</p></article></section>
    ${canManageEvent ? `<section id="engagement" class="mt-6 scroll-mt-24 rounded-[2rem] border border-[#dee7e3] bg-[#f0f5f2] p-5 sm:p-7"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-[#2f6b5b]">${el ? "Εμπειρία καλεσμένων" : "Guest experience"}</p><h2 class="mt-2 text-3xl">${el ? "Συμμετοχή, live προβολή & QR" : "Engagement, live display & QR"}</h2></div><div class="mt-5 grid gap-3 sm:grid-cols-3"><a href="/dashboard/${event.code}/engagement?lang=${locale}" class="rounded-2xl border border-[#dce6e2] bg-white p-5 transition hover:-translate-y-0.5 hover:shadow-md"><span class="text-2xl">✦</span><h3 class="mt-3 font-semibold">RSVP · Guestbook · Comments</h3><p class="mt-2 text-sm leading-6 text-[#697b74]">${el ? "Διαχείριση απαντήσεων και περιεχομένου." : "Manage responses and guest content."}</p></a><a href="/gallery/${event.code}/slideshow?lang=${locale}" target="_blank" class="rounded-2xl border border-[#dce6e2] bg-white p-5 transition hover:-translate-y-0.5 hover:shadow-md"><span class="text-2xl">▶</span><h3 class="mt-3 font-semibold">Live slideshow</h3><p class="mt-2 text-sm leading-6 text-[#697b74]">${el ? "Πλήρης οθόνη με αυτόματες νέες λήψεις." : "Fullscreen display with automatic new uploads."}</p></a><a href="/dashboard/${event.code}/qr-templates?lang=${locale}" class="rounded-2xl border border-[#dce6e2] bg-white p-5 transition hover:-translate-y-0.5 hover:shadow-md"><span class="text-2xl">▦</span><h3 class="mt-3 font-semibold">QR Studio</h3><p class="mt-2 text-sm leading-6 text-[#697b74]">${el ? "Έτοιμα σχέδια για εκτύπωση." : "Ready-to-print guest templates."}</p></a></div></section>` : ""}
    <section id="share" class="mt-6 scroll-mt-24 rounded-[2rem] bg-[#e9f1ed] p-5 sm:p-7"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-[#2f6b5b]">${el ? "Κοινοποίηση" : "Share"}</p><h2 class="mt-2 text-3xl text-[#183c33]">${el ? "Links και QR codes" : "Links and QR codes"}</h2><p class="mt-2 max-w-3xl text-sm leading-6 text-[#687a74]">${el ? "Η wedding σελίδα συγκεντρώνει την εμπειρία. Το guest gallery δέχεται uploads και το official album προβάλλει το επιλεγμένο υλικό." : "The wedding website brings the experience together. The guest gallery accepts uploads and the official album presents curated media."}</p></div><div class="mt-6 grid items-stretch gap-4 ${weddingUrl ? "lg:grid-cols-3" : "lg:grid-cols-2"}">${weddingUrl && weddingQrSvg ? shareCard("Wedding website", el ? "Η ενιαία, προσωποποιημένη σελίδα με όλες τις πληροφορίες και λειτουργίες." : "The unified, customizable page with all information and experiences.", weddingUrl, weddingQrSvg, "wedding-link", "website") : ""}${shareCard(el ? "Guest gallery & uploads" : "Guest gallery & uploads", el ? "Για καλεσμένους, φίλους και συνεργάτες που θέλουν να δουν ή να ανεβάσουν στιγμές." : "For guests, friends, and collaborators who want to view or upload moments.", guestUrl, guestQrSvg, "guest-link", "guest")}${shareCard(el ? "Official album" : "Official album", el ? "Η καθαρή παρουσίαση του επίσημου, επιλεγμένου album." : "The polished presentation of the official curated album.", officialUrl, officialQrSvg, "official-link", "official")}</div></section>

    <section id="gallery" class="mt-6 scroll-mt-24 rounded-[2rem] border border-[#dee7e3] bg-white p-5 shadow-sm sm:p-7"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-[#2f6b5b]">${el ? "Υλικό event" : "Event media"}</p><h2 class="mt-2 text-3xl text-[#183c33]">Gallery</h2>${galleryFilterControls(photoItems, "owner-gallery", locale)}</div><div class="flex flex-wrap items-center gap-2"><button id="owner-select-media" class="rounded-xl border border-[#d6e0dc] px-4 py-2.5 text-sm font-semibold">${el ? "Επιλογή" : "Select"}</button><button id="owner-download-selected" class="hidden rounded-xl bg-[#2f6b5b] px-4 py-2.5 text-sm font-semibold text-white">${el ? "Λήψη επιλεγμένων" : "Download selected"}</button>${canManageMedia ? `<button id="owner-delete-selected" class="hidden rounded-xl border border-red-200 px-4 py-2.5 text-sm font-semibold text-red-700">${el ? "Διαγραφή επιλεγμένων" : "Delete selected"}</button>` : ""}</div></div><form id="owner-bulk-media" action="/api/account/events/${event.code}/media/bulk-trash" method="post"><input type="hidden" name="locale" value="${locale}"><input id="owner-media-ids" type="hidden" name="ids"></form>${photoItems.length ? `<div data-gallery-grid="owner-gallery" class="mt-6 grid grid-cols-2 gap-3 sm:gap-4 md:grid-cols-3 lg:grid-cols-4">${ownerGalleryCards}</div>${galleryProgressiveControls(photoItems.length, "owner-gallery", locale)}` : `<div class="mt-6 rounded-2xl border border-dashed border-[#ceddd7] bg-[#fafcfb] px-6 py-16 text-center"><p class="text-lg font-semibold text-[#344941]">${el ? "Δεν υπάρχουν φωτογραφίες ακόμη" : "No photos yet"}</p><p class="mt-2 text-sm text-[#6c7b76]">${el ? "Μοιράσου το guest link ή QR για να ξεκινήσουν τα uploads." : "Share the guest link or QR code to start collecting uploads."}</p></div>`}</section>
    <div class="mt-6 space-y-6">${people}${requests}${danger}</div>
  </main>
  <script>(()=>{document.querySelectorAll('[data-copy-link]').forEach(button=>button.addEventListener('click',async()=>{const input=document.getElementById(button.dataset.copyLink);if(!input)return;await navigator.clipboard.writeText(input.value);const label=button.querySelector('span'),previous=label?.textContent;if(label)label.textContent=${JSON.stringify(el ? "Αντιγράφηκε" : "Copied")};setTimeout(()=>{if(label)label.textContent=previous},1400)}))})()<\/script>
  ${canManageEvent ? eventPinDialog(locale) : ""}${locationEnhancement}${inlineEditScript}${ownerSelectionScript}${galleryFilterScript(photoItems, "owner-gallery")}${galleryProgressiveScript("owner-gallery")}${lightboxMarkup(locale, true)}${mediaCommentsOverlay(event.code, locale)}${mediaLikesScript(event.code, locale)}${logoutScript(locale)}`;

  return page(event.eventName, body);
}
